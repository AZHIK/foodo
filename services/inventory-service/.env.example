# ──────────────────────────────────────────────
# FoodLink Inventory Service — Environment Variables
# ──────────────────────────────────────────────
# Copy to .env and fill in real values for local dev.
# Docker Compose reads this file automatically.

# ── App ────────────────────────────────────────
ENVIRONMENT=local                          # local | dev | staging | prod
DEBUG=true
SERVICE_NAME=inventory-service
LOG_LEVEL=DEBUG                            # DEBUG | INFO | WARNING | ERROR

# ── PostgreSQL ────────────────────────────────
DB_URL=postgresql+asyncpg://foodlink:foodlink@localhost:5432/foodlink_inventory

# Separate test database — used by the test suite (real alembic upgrade head).
# Leave unset for normal dev; the test suite sets it explicitly.
TEST_DB_URL=postgresql+asyncpg://foodlink:foodlink@localhost:5432/foodlink_inventory_test

# ── JWT (public key only — verify tokens, never issue them) ──
JWT_PUBLIC_KEY_PATH=keys/public.pem

# ── RabbitMQ (shared event bus) ───────────────
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
EVENTS_EXCHANGE=foodlink.events

# ── Host ──────────────────────────────────────
HOST=0.0.0.0
PORT=8100