/// ── shared/models/ ─────────────────────────────────────────────────
///
/// Shared domain models used across multiple features (e.g. Money,
/// Uuid, PaginatedList, Address).
///
/// Populated as cross-cutting model needs are identified.
const String _sharedModelsReadme = '';
