/// ── shared/widgets/ ────────────────────────────────────────────────
///
/// Reusable widgets used across features (e.g. AppButton, SyncStatusBadge,
/// EmptyStateView, LoadingOverlay).
///
/// Populated as common UI patterns emerge during feature development.
const String _sharedWidgetsReadme = '';
