/// ── inventory/ ─────────────────────────────────────────────────────
///
/// Feature: Stock management — item catalog, stock levels, adjustments,
/// low-stock alerts, inventory counts.
///
/// Populated in Stage 4 (inventory UI + local tables).
const String _inventoryReadme = '';
