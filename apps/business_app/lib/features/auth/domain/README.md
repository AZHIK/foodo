/// ── auth/domain ────────────────────────────────────────────────────
///
/// This directory will contain:
///   - Entities / value objects
///   - Repository interfaces (abstract classes)
///   - Use-cases
///
/// Populated in Stage 2 (auth integration).
const String _authDomainReadme = '';
