/// ── auth/data ──────────────────────────────────────────────────────
///
/// This directory will contain:
///   - Data sources (remote: Identity Service API client; local: Drift DAOs)
///   - DTOs / request / response models
///   - Repository implementations
///
/// Populated in Stage 2 (auth integration).
const String _authDataReadme = '';
